public class WarGameApp {

    /**
     * main method that starts the program, needed because a main that starts the program shouldn't inherit Application class
     * @param args
     */
    public static void main(String[] args) {
        WarGamesGUI.main(args);
    }
}
